<%= form_for([:admin, @group])  do |f| %>
  <%=f.error_messages  %>
  <%= hidden_field_tag 'parent_id', params[:parent_id] %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
